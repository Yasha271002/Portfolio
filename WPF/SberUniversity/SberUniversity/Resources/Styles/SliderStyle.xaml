<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SberUniversity.Helpers">
    <ControlTemplate x:Key="CommonRepeatButtonTemplate" TargetType="RepeatButton">
        <Border BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}"
                Height="{TemplateBinding Height}"
                Opacity="{TemplateBinding Opacity}"
                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:SliderCornerRadiusProperties.ScrollBarCornerRadius)}">
        </Border>
    </ControlTemplate>

    <Style x:Key="BaseSliderRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template" Value="{StaticResource CommonRepeatButtonTemplate}"/>
    </Style>

    <Style x:Key="SliderRepeatButton" BasedOn="{StaticResource BaseSliderRepeatButtonStyle}" TargetType="RepeatButton">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="Black" />
        <Setter Property="Opacity" Value="0.5" />
        <Setter Property="Height" Value="12" />
        <Setter Property="local:SliderCornerRadiusProperties.ScrollBarCornerRadius" Value="0 5 5 0"/>
    </Style>

    <Style x:Key="SliderRepeatButton1" BasedOn="{StaticResource BaseSliderRepeatButtonStyle}" TargetType="RepeatButton">
        <Setter Property="Height" Value="24" />
        <Setter Property="local:SliderCornerRadiusProperties.ScrollBarCornerRadius" Value="16 0 0 16"/>
        <Setter Property="Margin" Value="20 0 -20 0"/>
        <Setter Property="Background" Value="{DynamicResource MainTextColorBrush}" />
    </Style>

    <Style x:Key="SliderRepeatButton2" BasedOn="{StaticResource BaseSliderRepeatButtonStyle}" TargetType="RepeatButton">
        <Setter Property="Height" Value="24" />
        <Setter Property="Margin" Value="-20 0 20 0"/>
        <Setter Property="local:SliderCornerRadiusProperties.ScrollBarCornerRadius" Value="0 16 16 0"/>
        <Setter Property="Background" Value="White" />
        <Setter Property="Opacity" Value="0.5" />
    </Style>

    <ControlTemplate x:Key="CommonThumbTemplate" TargetType="Thumb">
        <Ellipse Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" Fill="{TemplateBinding Background}"></Ellipse>
    </ControlTemplate>

    <Style x:Key="BaseSliderThumbStyle" TargetType="Thumb">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template" Value="{StaticResource CommonThumbTemplate}"/>
    </Style>

    <Style x:Key="SliderThumb" BasedOn="{StaticResource BaseSliderThumbStyle}" TargetType="Thumb">
        <Setter Property="Height" Value="51" />
        <Setter Property="Width" Value="51" />
        <Setter Property="Margin" Value="-2 0 0 0"/>
        <Setter Property="Background">
            <Setter.Value>
                <ImageBrush ImageSource="../Images/SliderCircle.png"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="QuizSliderThumb" BasedOn="{StaticResource BaseSliderThumbStyle}" TargetType="Thumb">
        <Setter Property="Height" Value="51" />
        <Setter Property="Width" Value="51" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Direction="135"
                                  ShadowDepth="4"
                                  Color="Black"
                                  Opacity="0.15"
                                  BlurRadius="10"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Background">
            <Setter.Value>
                <ImageBrush ImageSource="../Images/SliderCircle.png"/>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="SliderTemplate" TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Track Grid.Row="1" x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1}"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton}"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="QuizSliderTemplate" TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border x:Name="TrackBackground" CornerRadius="16" Height="24" Margin="5,0" Grid.Row="1" VerticalAlignment="center" Background="White" Opacity="0.2">
            </Border>
            <Track Grid.Row="1" x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1}"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource QuizSliderThumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton2}"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="BaseHorizontalSliderStyle" TargetType="Slider">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinHeight" Value="21" />
                <Setter Property="MinWidth" Value="104" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Horizontal_Slider" TargetType="Slider" BasedOn="{StaticResource BaseHorizontalSliderStyle}">
        <Setter Property="Template" Value="{StaticResource SliderTemplate}" />
    </Style>

    <Style x:Key="Horizontal_QuizSlider" TargetType="Slider" BasedOn="{StaticResource BaseHorizontalSliderStyle}">
        <Setter Property="Template" Value="{StaticResource QuizSliderTemplate}" />
    </Style>
</ResourceDictionary>