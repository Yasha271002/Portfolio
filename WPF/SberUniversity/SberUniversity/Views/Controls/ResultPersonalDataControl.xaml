<UserControl x:Class="SberUniversity.Views.Controls.ResultPersonalDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SberUniversity.Views.Controls"
             xmlns:views="clr-namespace:SberUniversity.ViewModel.Views"
             xmlns:helpers="clr-namespace:SberUniversity.Helpers"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance views:ResultPageViewModel}">
    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="EmailTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border
                            BorderBrush="White"
                            BorderThickness="2"
                            Padding="0"
                            CornerRadius="37">
                            <Border.Background>
                                <SolidColorBrush Color="White" Opacity="0.1" />
                            </Border.Background>
                            <Grid>
                                <Grid Margin="{TemplateBinding Padding}">
                                    <TextBlock
                                        Text="{TemplateBinding Tag}"
                                        Opacity="0.8"
                                        FontSize="24"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        Margin="52 0 0 0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock"
                                                   BasedOn="{StaticResource DefaultTextBoxStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"></Setter>
                                                <Style.Triggers>
                                                    <DataTrigger
                                                        Binding="{Binding Text.Length,RelativeSource={RelativeSource FindAncestor,AncestorType=TextBox} }"
                                                        Value="0">
                                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                                <ScrollViewer
                                    Padding="0"
                                    Margin="52 0 0 0"
                                    x:Name="PART_ContentHost">
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OptionMarkFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="14,0,0,0" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource MainTextColorBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Height="32"
                                Width="32"
                                CornerRadius="6">
                                <ContentPresenter ContentTemplate="{StaticResource SuccessIcon}"
                                                  Name="ContentPresenter"
                                                  Visibility="Collapsed"
                                                  Height="20"
                                                  Width="20"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="ContentPresenter" Property="Visibility" Value="Visible"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="{StaticResource White}"
                                                            Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                                            Duration="0:0:0.5">
                                                <ColorAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </ColorAnimation.EasingFunction>
                                            </ColorAnimation>
                                            <ThicknessAnimation To="0"
                                                             Storyboard.TargetProperty="BorderThickness"
                                                             Duration="0:0:0.5">
                                                <ThicknessAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                            <DoubleAnimation To="1"
                                                            Storyboard.TargetProperty="Opacity"
                                                            Storyboard.TargetName="ContentPresenter"
                                                            Duration="0:0:0.5">
                                                <DoubleAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="Transparent"
                                                            Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                                            Duration="0:0:0.5">
                                                <ColorAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </ColorAnimation.EasingFunction>
                                            </ColorAnimation>
                                            <ThicknessAnimation To="1"
                                                                Storyboard.TargetProperty="BorderThickness"
                                                                Duration="0:0:0.5">
                                                <ThicknessAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                            <DoubleAnimation To="0"
                                                             Storyboard.TargetProperty="Opacity"
                                                             Storyboard.TargetName="ContentPresenter"
                                                             Duration="0:0:0.5">
                                                <DoubleAnimation.EasingFunction>
                                                    <PowerEase Power="2"
                                                               EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel HorizontalAlignment="Center">
        <TextBox Style="{StaticResource EmailTextBox}"
                 Width="758"
                 Height="74"
                 Text="{Binding Result.EMail,Mode=TwoWay
                        ,UpdateSourceTrigger=PropertyChanged}"
                 Tag="E-mail"
                 Foreground="White"
                 CaretBrush="White"
                 FontSize="24"
                 HorizontalContentAlignment="Left"
                 VerticalContentAlignment="Center" />
        <StackPanel Orientation="Horizontal" Margin="0 16">
            <CheckBox Style="{DynamicResource CheckBoxStyle}"
                      Margin="0 0 16 0"
                      Focusable="False"
                IsChecked="{Binding Result.IsAgreePersonalData, Mode=TwoWay
                        ,UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock
                Width="688"
                TextWrapping="Wrap"
                Style="{StaticResource DefaultTextBoxStyle}" FontSize="20">
                <Run Text="Я ознакомлен с "/>
                <Hyperlink Command="helpers:CommonCommands.OpenPopupCommand"
                           Foreground="White"
                           CommandParameter="{x:Static helpers:PopupTypes.PersonalDataPopup}">
                    Политикой СберУниверситета
                </Hyperlink>
                <Run Text="и даю согласие 
                     на"/>
                <Hyperlink Command="helpers:CommonCommands.OpenPopupCommand"
                            Foreground="White"
                            CommandParameter="{x:Static helpers:PopupTypes.UserAgreements}">
                    обработку персональных данных
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </StackPanel>
</UserControl>